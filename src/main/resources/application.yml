server:
  shutdown: graceful

spring:
  lifecycle:
    timeout-per-shutdown-phase: 20s
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#        format_sql: true
  datasource:
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://localhost:13306/shop_cms?serverTimezone=UTC&characterEncoding=UTF-8
      username: shop-svc
      password: shop-pass
  flyway:
    user: shop-svc
    password: shop-pass
    schemas: shop_cms
    url: jdbc:mysql://localhost:13306/shop_cms?serverTimezone=UTC&characterEncoding=UTF-8
    enabled: true

#  h2:
#    console:
#      enabled: true
#      path: /h2-console

  # Redis
  redis:
    host: localhost
    port: 6379

  # Swagger
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  mail:
    host: smtp.gmail.com
    port: 587
    username: jinhun3892@gmail.com
    password: wlsgns12
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  config: classpath:logback-local.xml
#  level:
#    org:
#      hibernate:
#        type:
#          descriptor:
#            sql: trace

jwt:
  secret: VlwEyVBsYt9V7zq57TejMnVUyzblYcfPQye08f7MGVA9XkHN
